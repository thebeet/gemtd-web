# Browser-side WebSocket endpoint. Leave unset for local development.
# VITE_YJS_URL=wss://your-domain.example/yjs
# Battle endpoint is independent of the Yjs endpoint.
# VITE_GAME_URL=wss://your-domain.example/game-sync
